services:
  devcontainer:
    env_file:
      - path: ../.local/.env
        required: false
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    volumes:
      - WORKSPACES:/workspaces
      - ..:/workspaces/features
volumes:
  WORKSPACES:
